<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Legacy concatenated file — entities live in individual files under subfolders -->
    <Compile Remove="Entities\Entities.cs" />
  </ItemGroup>

</Project>
