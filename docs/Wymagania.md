### **Zakres materiału**

1. Zrealizowane funkcjonalności:  
       \-    (1 pkt za 1 funkcjonalność, max 15 pkt)   
       \- funkcjonalności mają spełniać praktyki wydajnego programowanie w .net: wzorzec repozytorium, dto itd.  
        \-    Do jednej funkcjonalności zaliczane są operacje CRUD (np. tworzenie, aktualizacja, wyświetlanie i usuwanie kategorii)  
2. Wdrożenie modeli z wykorzystaniem relacji ManyToMany (przynajmniej 1\) i OneToMany . Ok 20 modeli/encji (6-7 os.) z wykorzystaniem ORM (w zależności od zaawansowania i skomplikowania relacji w bazie danych: max 10 pkt)  
3. Realizacja uwierzytelniania z wykorzystaniem modułu ClaimsIdentity. Projekt powinien wspierać przynajmniej dwie role: użytkownika zalogowanego oraz moderatora z wyższymi uprawnieniami. (10 pkt)  
4. Realizacja przesyłania plików na serwer lokalny lub zdalny z wykorzystaniem formatki HTML file (5 pkt).  
5. Estetyczny wygląd aplikacji (5 pkt)  
6. Dokumentacja i jej użyteczność (2 pkt)  
7. Testy automatyczne (max 8 pkt). Pokrycie testami 70-80% głównej logiki aplikacji (bez zapisów do ORM)  
8. Wdrożenie aplikacji na produkcji (max 5pkt)  
9. Zastosowanie zewnętrznego API np. pobranie prognozy pogody,  zastosowanie OpenAI, płatności itp. (max. 5 pkt)  
10. Aplikacja webowa powinna stosować jedną z poznanych w trakcie studiów architektur projektów IT np. architekturą warstwową, wykorzystującą wzorce Repository, Dependency Injection oraz Data Transfer Object (DTO) dostosowaną do wymagań biznesowych, skalowalności i złożoności systemu.

Dodatkowo:

1. Aplikacja posiada obsługę formularzy do wyświetlania, usuwania, dodawania i edycji elementów  
2. Aplikacja posiada moduł logowania, obsługuję autoryzację użytkownika  
3. Aplikacja odwołuje się do Api zewnętrznego (pogoda, kursy, akcje, autoryzacja google/fb)  
4. Aplikacja udostępnia interfejs API  
5. Aplikacja posiada bazę danych oraz przechowuje trwale informacje  
6. Liczność encji nie słownikowych przekracza 7/osoba w grupie \- minimalna ilość encji ogółem wynosi 10/osoba  
7. Aplikacja jest przejrzysta i estetyczna (zastosowanie zmian w stylizowaniu)  
8. Aplikacja obsługuje załączniki (w formularzach oraz umożliwia pobieranie plików)  
9. Aplikacja Generuje dokumenty (np.: pdf, excel, word) \- można wybrać 1 typ dokumentu

